---
title: Hello, world
description: The first post — replace it with your own.
tags: [meta]
---

This is a post. It lives in `_posts/`, named `YYYY-MM-DD-title.md`, and the
front matter above is all it needs — the layout, the sidebar, the share row
and the structured data all come from the theme.

A few things worth trying while this post is still disposable:

- A code block gets a language label and a copy button:

```apex
public with sharing class HelloWorld {
    public static String greet(String name) {
        return 'Hello, ' + name;
    }
}
```

- The [components](https://trailblazer.imswarnil.com/docs/components/) —
  callouts, tabs, steps and stats — work inside any Markdown file.
- Delete this file when you publish your first real post.
