# Jekyll build output & caches
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Bundler — installed gems should never be committed
.bundle/
vendor/

# Dependency lockfile (resolved fresh by CI)
Gemfile.lock
**/Gemfile.lock

# Built gem packages and marketplace zips
*.gem
dist/

# OS / editor cruft
.DS_Store
*.log
node_modules/
